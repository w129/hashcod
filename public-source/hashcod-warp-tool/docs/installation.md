# Installation

The browser adapter is served as a static Hashcod module. It has no secrets and
requires no database migration.

For native Warp development:

```powershell
./scripts/fetch-upstream.ps1
cd upstream-warp
./script/bootstrap
./script/run
```

Follow the upstream platform instructions at https://docs.warp.dev/.
